'use client'
import { useState } from 'react';import { useRouter } from 'next/navigation';import Link from 'next/link';import { api } from '@/lib/api';import { useAuth } from '@/components/auth-context';import { Shell } from '@/components/ui'
export default function Auth(){const [register,setRegister]=useState(false);const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();const auth=useAuth();async function submit(e:any){e.preventDefault();setBusy(true);setError('');try{if(register)await api.register({email,password,full_name:name});await auth.login(email,password);router.push('/evaluate')}catch(x:any){setError(x.message||'Unable to continue.')}finally{setBusy(false)}}return <Shell><main className="mx-auto flex max-w-md flex-col px-6 pb-24 pt-16"><Link href="/" className="mb-12 text-sm text-muted-foreground hover:text-foreground">← Back to UniPath</Link><p className="text-sm font-semibold text-primary">{register?'Create your account':'Welcome back'}</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">{register?'Start with your profile.':'Continue your path.'}</h1><p className="mt-3 leading-6 text-muted-foreground">{register?'Your personalized shortlist is a few details away.':'Pick up where you left off.'}</p><form onSubmit={submit} className="mt-9 flex flex-col gap-5">{register&&<label className="flex flex-col gap-2 text-sm font-medium">Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Avery Chen"/></label>}<label className="flex flex-col gap-2 text-sm font-medium">Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className="flex flex-col gap-2 text-sm font-medium">Password<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<p className="text-sm text-primary">{error}</p>}<button disabled={busy} className="rounded-lg bg-primary px-5 py-3.5 font-semibold text-primary-foreground disabled:opacity-60">{busy?'Please wait…':register?'Create account':'Sign in'}</button></form><button onClick={()=>setRegister(!register)} className="mt-6 text-center text-sm text-muted-foreground hover:text-foreground">{register?'Already have an account? Sign in':'New to UniPath? Create an account'}</button></main></Shell>}
